# CI dependencies — lint + unit tests only.
# Does NOT include GPU/ML packages (faster-whisper, torch, transformers).
# Those are stubbed in tests/conftest.py when not installed.
pyyaml>=6.0
requests>=2.31.0
pytest>=7.4.0
flake8>=6.1.0
