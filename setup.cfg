[flake8]
max-line-length = 100
max-doc-length = 120
exclude =
    venv,
    .git,
    __pycache__,
    *.egg-info
per-file-ignores =
    # test files use longer lines in fixtures and parametrize lists
    tests/*:E501
